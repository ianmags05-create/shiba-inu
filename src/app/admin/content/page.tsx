import { createAdminClient } from "../../../lib/supabase/admin";
import { requireAdmin } from "../../../lib/admin-auth";
import AdminShell from "../admin-shell";
import { deleteContent, saveContent } from "../crud-actions";
import styles from "../admin.module.css";
export const dynamic="force-dynamic";
export default async function ContentPage(){const user=await requireAdmin();const {data=[]}=await createAdminClient().from("site_content").select("*").order("sort_order");return <AdminShell title="Website Content" email={user.email} active="/admin/content"><div className={styles.toolbar}><p>Edit text displayed on the public homepage.</p></div><section className={styles.formCard}><form action={saveContent}><label>Key<input name="content_key" placeholder="section.field" required/></label><label>Label<input name="label" required/></label><label>Section<input name="section" defaultValue="General"/></label><label>Type<select name="content_type"><option>text</option><option>textarea</option><option>url</option><option>phone</option><option>email</option></select></label><label className={styles.wide}>Value<textarea name="value"/></label><label>Order<input name="sort_order" type="number" defaultValue="0"/></label><label className={styles.check}><input name="published" type="checkbox" defaultChecked/>Published</label><button type="submit">Add content field</button></form></section><div className={styles.grid}>{(data||[]).map((item)=><article className={styles.row} key={item.id}><form action={saveContent}><input type="hidden" name="id" value={item.id}/><label>Key<input name="content_key" defaultValue={item.content_key} required/></label><label>Label<input name="label" defaultValue={item.label} required/></label><label>Section<input name="section" defaultValue={item.section}/></label><label>Type<select name="content_type" defaultValue={item.content_type}><option>text</option><option>textarea</option><option>url</option><option>phone</option><option>email</option></select></label><label className={styles.wide}>Value<textarea name="value" defaultValue={item.value}/></label><label>Order<input name="sort_order" type="number" defaultValue={item.sort_order}/></label><label className={styles.check}><input name="published" type="checkbox" defaultChecked={item.published}/>Published</label><button>Save changes</button><button className={styles.danger} formAction={deleteContent}>Delete</button></form></article>)}</div></AdminShell>}
